import { Neutron } from "../neutron/Neutron.ts";

export class Events extends Neutron.Events {
  onClick(e: KeyboardEvent) {
    switch (e.key) {
      case ``:
        break;
    }
  }

  offClick(e: KeyboardEvent) {
    switch (e.key) {
      case ``:
        break;
    }
  }
}
